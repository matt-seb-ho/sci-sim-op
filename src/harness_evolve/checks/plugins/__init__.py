"""Check plugins that ship with the package.

Every file here is subject to the same fence as a candidate-authored plugin --
sibling test, fixed interface, subprocess vetting, five-second budget. They are
not privileged; they are worked examples of what a proposer is being asked to
produce, and ``tests/test_checks.py`` runs the real fence over them so the
examples cannot rot into something the fence would reject.
"""
